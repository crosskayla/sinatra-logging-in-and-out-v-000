<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>title</title>
    <link rel="stylesheet" href="stylesheets/bootstrap.css" type="text/css">
  </head>
  <body id="page-top">

  <% if is_logged_in?(@session) %>
    <h2>Username: <%= current_user(@session).username %>
    <h2>Your Balance:</h2>
    <p><%= current_user(@session).balance %></p>

    <br>
    <a href="/logout">Log Out</a>
  <% end %>

  </body>
</html>
